"""v2 agentic pipeline: a reusable LLM workflow runtime plus the tasks built on it.

The same machinery turns messy free text into a caller-pinned table for both the
eligibility and drug-utility problems. See docs/v2_agentic_pipeline_spec.md.
"""
